<!DOCTYPE html>
<html>

<head>
  <title>Edit Post</title>
</head>

<body>
  <h1>Edit Post</h1>
  <form action="/dashboard/edit/{{post.dataValues.id}}" method="POST">
    <div>
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" value="{{post.dataValues.title}}">
    </div>
    <div>
      <label for="content">Content:</label>
      <textarea id="content" name="content" rows="5">{{post.dataValues.content}}</textarea>
    </div>
    <button type="submit">Update Post</button>
  </form>
</body>

</html>
